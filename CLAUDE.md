# comedu_portal — 영역으로 찾는 정보 수업 도구함 (티쳐무의 정보교육 포털)

## ① 목적
- 티쳐무가 만든 수업 자료(웹교과서 · 수업교안 노트 · 영역별 학습 자료 · 제출형 활동지 · 교사·학급 도구)를
  2022 개정 「정보」 영역 순서(컴퓨팅 시스템 → 데이터 → 알고리즘과 프로그래밍 → 인공지능 → 디지털 문화)로
  한 페이지에 모은 포털이다.
- 보는 사람은 학생과 교사다. **다른 학교 선생님도 본다** — 그래서 제출형 활동지는 「교사용 사본」으로 나눈다.

## ② 파일과 기능
| 파일 | 하는 일 |
|---|---|
| `index.html` | 포털 전부(CSS·JS 인라인). **`DATA` 배열이 카드 목록**이다 |
| `guide-figures.js` | 배포 안내 그림(SVG) 5가지. 글자(SVG 문자열)만 만들고 DOM 은 쓰지 않는다 |
| `tools/export-guide-images.mjs` | 안내 그림을 PNG 로 뽑는다 (이 PC 의 Edge 헤드리스, 설치할 것 없음) |
| `guide/` | 뽑아 둔 PNG — 공통 4장 + 앱별 메뉴 그림 + 앱별 한 장짜리 전체 안내(`<key>-guide.png`). 안내 화면의 [🖼 그림 한 장으로 보기] 가 이것을 연다 |
| `netlify.toml` | Netlify 설정 (`publish = "."`) |

- 카드 넣기: `DATA` 의 알맞은 칸 `items` 에 `{ icon, title, desc, url }` 한 줄. 자료가 없는 칸은 `soon` 글이 「준비 중」 카드가 된다.
- 위쪽 거르기 단추에는 `chip`(짧은 이름), 칸 제목에는 `cat` 을 쓴다.

### ★ 제출형 활동지 — 교사용 사본 (2026-09-19)
다른 학교 선생님이 우리 Apps Script 앱을 **자기 드라이브에 사본으로 만들어** 쓰게 한다.
학생 답이 그 선생님 시트에만 쌓이고 우리 계정을 거치지 않는다
(「내 웹앱 하나에 여러 선생님 시트 주소를 넣는」 방식은 남의 학교 학생 정보가 우리 계정을 거치고
동시 실행 한도(사용자당 30개)를 여러 학교가 나눠 써서 택하지 않았다).

**흐름 (사용자 지시 2026-09-19)**
1. 카드(아무 데나)를 누르면 **고르는 창**(`dialog#panel`) — [🧑‍🏫 교사용 사본 만들기] · [▶ 테스트 페이지] ·
   「사본을 만들기 전에 배포 방법부터 읽어 보기」. 카드 위의 두 단추로도 바로 갈 수 있다.
2. **[교사용 사본 만들기]** → 구글의 사본 화면(`…/copy`)은 **새 탭**으로 열리고,
   **지금 탭은 배포까지 따라 하는 안내 화면**(`main#guideView`, 주소 `#copy-<key>`)으로 넘어간다.
   안내 화면은 카드 목록(`#portalView`) 자리에 뜨고, 「새 탭에서 [사본 만들기]를 누른 뒤 2단계부터」 띠가 붙으며 1단계가 체크된다.
3. 안내 화면: 1 사본 만들기(다시 열기·링크 복사) → 2 앱 메뉴에서 시트 준비·권한 승인 → 3 웹 앱 배포,
   그림 5장 · 단계 체크 · 「꼭 알아 두세요」 · [🔗 이 안내 링크 복사] · [🖼 그림 한 장으로 보기] · [← 도구함으로 돌아가기].
- 새 탭은 `<a target="_blank">` 가 그대로 연다(사용자 클릭이라 팝업 차단에 걸리지 않는다).
  안내 화면으로 넘기는 일은 `setTimeout(…,0)` 으로 링크의 기본 동작 **뒤에** 한다.
- 뒤로 가기: 카드에서 넘어가면 `pushState` 로 기록을 남겨 브라우저 [뒤로] 와 [← 도구함으로] 가 카드 목록으로 돌아가고
  스크롤 자리도 되돌린다. `#copy-<key>` 링크로 바로 들어온 경우에는 `replaceState` 로 주소만 바꾼다.

**`teacher` 값** (자세한 설명은 `index.html` 주석)
| 이름 | 뜻 |
|---|---|
| `key` | 안내 화면 주소 `#copy-<key>` · PNG 파일 이름 |
| `sheet` | 배포용 원본의 사본 링크(`…/copy`). `/edit?usp=sharing` 이나 ID 만 적어도 된다. 비우면 「원본 준비 중」으로 잠긴다 |
| `test` | 둘러보기용 테스트 페이지(웹앱 `/exec`) |
| `menu` · `setup` · `reset` | 그 앱 `Code.gs` 의 `createMenu`·`addItem` 글자 **그대로** |
| `drive` | 드라이브 권한도 묻는 앱이면 `true` (지금은 검색어 트렌드 = `trend_bigdata`) |

#### 등록 현황 (2026-09-19, 사용자가 준 링크)
| key | 카드 이름 | 앱 폴더 | 원본 시트 ID | 원본 공개 |
|---|---|---|---|---|
| `sw-idea` | SW 개발 아이디어 제안하기 | `sw-idea/` | `1Tm4mJKw2UZsfD5sYCcobNGDZov9uhzMEe5t1fB8uXFk` | ✅ 200 |
| `open-data` | 공공데이터 수집 기록장 | `open_data/` | `1xr_rgURmxdM_Lt78nqoFoM-rzPhlU4rBFgk_L_LVuW0` | ⚠ 401 |
| `trend-bigdata` | 검색어 트렌드 분석하기 | `trend_bigdata/` | `1-XZOnMdGDg4Qofg1jK3Nv9UvfaRJnA8VJ0fKqMjgtuY` | ⚠ 401 |
| `data-structure` | 자료구조 실습실 | `data_structure_basics/` | `1EmMJboqcB82SfpizwqM60e98UEsGibSUEkNhC9acLSk` | ⚠ 401 |
| `data-science-career` | 데이터 과학의 영향력 탐구 (진로연계) | `data_science_career/` | `1K4bw1kxPL8bFdl8xUsTI_VoOi3PrkciPDMf-aeWapCI` | ⚠ 401 |
- 테스트 페이지 다섯 곳은 모두 로그인 없이 열린다(200, 제목 확인).
- **⚠ 2026-09-19 현재 원본 넷이 「링크가 있는 모든 사용자」로 공유되어 있지 않다** — 로그인 없이 `/edit` 를 열면 401
  「이 콘텐츠에 액세스하려면 로그인해야 합니다」. 이대로면 다른 학교 선생님이 [사본 만들기] 를 누를 수 없다.
  확인: `curl -s -o /dev/null -w "%{http_code}" https://docs.google.com/spreadsheets/d/<ID>/edit` 가 **200** 이면 공개
  (`/copy` 는 로그인이 필요해 원래 302 다). 학교(Workspace) 계정은 도메인 밖 공유가 막혀 있을 수 있다.
- 옛 카드 링크(티쳐무 반 제출용 `/exec` 셋 — 공공데이터 goedu.kr 배포 · 자료구조 · 빅데이터)는 2026-09-19 사용자 지시로 모두 뺐다.

#### ⚠ 지킬 것
- **`sheet` 에는 반드시 학생 데이터가 없는 「배포용 원본」을 넣는다.** 운영 중인 시트를 넣으면
  숨긴 「계정(비밀번호)」 시트까지 복사되어 우리 반 학생의 학번·이름·답이 넘어간다.
- **배포용 원본은 배포하지 않는다.** 배포는 사본에 따라가지 않으므로 필요 없고, 원본을 배포하면
  원본 시트에 기록되는 주소가 생긴다. **테스트 페이지는 원본이 아닌 별도 사본에서 배포**해야 한다 —
  원본에서 배포하면 체험하는 사람의 계정·답이 원본에 쌓여 모든 사본으로 복사된다.
  (2026-09-19 SW 아이디어 원본에 「제출 현황」·「작성 중」 탭이 생겨 있었다. 칸 내용은 바깥에서 보이지 않아 확인 못 함)
- 원본의 **Apps Script 프로젝트 이름**이 권한 창의 「○○(으)로 이동(안전하지 않음)」에 그대로 나온다. 앱 이름으로 바꿔 둔다.
- 앱의 시트 메뉴 글자를 바꾸면 `DATA` 의 `menu`·`setup`·`reset` 도 고치고 그림을 다시 뽑는다.
- 앱 코드를 고치면 **배포용 원본의 스크립트도 함께 고쳐야** 새 사본에 들어간다. 이미 만든 사본은 안 바뀐다.
- 스크립트 속성(솔트·폴더 ID·마감 상태)은 사본에 따라가지 않는다(커뮤니티 확인, 공식 문서엔 없음).
  우리 앱들은 비어 있으면 새로 만들도록 짜여 있어 사본에서 그대로 돈다.

#### 안내 그림
- **실제 화면을 찍은 것이 아니라 다시 그린 안내도**다. 단추 이름은 2026-09 한국어 화면 기준
  (확장 프로그램 › Apps Script, 배포 › 새 배포, 유형 선택 ⚙ › 웹 앱, 다음 사용자 인증 정보로 실행, 액세스 권한이 있는 사용자,
  Google에서 확인하지 않은 앱 → 고급 → ○○(으)로 이동(안전하지 않음), 권한 체크 상자 「모두 선택」 → 계속).
  권한 창의 체크 상자(세분화된 동의)는 2025-01 편집기 실행, 2026-01 웹 앱으로 넓어졌다.
- 번호는 `HLN()` 으로 단다 — 테두리 모서리보다 바깥에 달아 글자를 가리지 않게 했고,
  바로 위에 이름표가 있는 넓은 칸(새 배포 창의 두 선택 칸)은 `'tr'`(오른쪽 위)로 단다.
- 그림을 고치면: `node tools/export-guide-images.mjs` → `guide/*.png` 가 새로 나온다.
  앱 이름·메뉴·사본 링크는 스크립트가 `index.html` 의 `DATA` 를 읽어 쓰므로 여기에 또 적지 않는다.

#### 만들며 겪은 것
- **크롬은 `<dialog>` 의 `close` 알림을 다음 화면 그리기 때 보낸다.** 탭이 가려져 있으면 한참 안 와서
  `html.locked`(뒤 스크롤 잠금)가 남았다. `closePanel()` 에서 바로 푼다.
- **휴대폰 폭에서 그림이 상자 밖으로 삐져나갔다** — 격자 칸이 `1fr` 이면 그림 최소 폭(560px)만큼 버틴다.
  `.step` 둘째 칸을 `minmax(0,1fr)`, `.s-body{min-width:0}` 로 두어 그림 칸 안에서 가로로 밀어 보게 했다.
- 대화상자가 열린 동안 바깥은 눌리지 않으므로, 복사용 임시 글상자도 열린 대화상자 안에 넣었다 뺀다.
- `[hidden]{display:none !important}` 를 두었다 — 안내 화면(`main.guide-page`)에 `display` 가 붙어 있어도 확실히 숨게.

## ③ 미리보기와 배포
- 미리보기: `.claude/launch.json` 의 `comedu-portal`(python `http.server` 5510).
  `file://` 로 열면 미리보기 창이 `data:` 로 바꿔 열어 `guide-figures.js` 를 못 읽는다(그림만 빠지고 글은 나온다).
- 저장소: https://github.com/trmoo/comedu_portal (Pages: https://trmoo.github.io/comedu_portal/). 빌드 없는 정적 사이트다(브랜치 배포).

## ④ 현재 상태와 다음 할 일
- 2026-09-19 — 교사용 사본 흐름(고르는 창 → 새 탭 사본 + 이 탭 안내 화면) · 제출형 활동지 5종 · 안내 그림 5장 · PNG 뽑기 도구.
  - 확인한 것: 옛 링크 3개가 페이지에서 사라짐 · 카드 5장의 사본·테스트 링크와 새 탭 · 고르는 창 · 사본 클릭 뒤 안내 화면 전환
    (띠·1단계 체크·그림 5장·주소 `#copy-<key>`) · 돌아가기와 브라우저 [뒤로] · 공유 링크로 바로 들어오기 · 그림 한 장 PNG 200 ·
    휴대폰 폭(375px) 넘침 없음 · 콘솔 오류 없음.
  - **확인하지 못한 것**: 실제로 새 탭이 뜨는 모습(미리보기 창이 가려져 실제 클릭 불가 — 링크 속성 `target="_blank"` 만 확인),
    [링크 복사] 의 실제 클립보드 동작.
- 다음: 원본 넷의 공유를 「링크가 있는 모든 사용자 – 뷰어」로 (사용자) → `curl` 로 200 확인.
- ⚠ 저작권: 이 저장소에는 아직 `LICENSE` 가 없다(루트 `CLAUDE.md` 「저작권 표시」 방침 미적용). 따로 정리할 일.
